name: tinychat

services:
  tinychat:
    build: .
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8080
      OPENAI_CHAT_HOST: ${OPENAI_CHAT_HOST:-}
